import Material from './Material';
import withTabs from '../../../common/withTabs';
import withGroup from '../../../common/withGroup';
const GroupMaterial = withGroup(Material);
export default withTabs('Material', GroupMaterial);
